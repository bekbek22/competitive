#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int lens = 100;

int main() {

    //char num[lens];
    //cin.getline (num, lens);
    string num;
    getline(cin, num);
    int len = num.length();
    
    for(int i = 0; i < len; i++) {

    }
    
}